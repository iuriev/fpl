# Tasks: Transfer Screen Polish

- [ ] Track `isDirty` state in `TransferScreen.tsx`.
- [ ] Update `TransferActionBar` props and implementation to handle disabled state for "Transfers" button.
- [ ] Implement responsive jersey sizing in `colors_and_type.css`.
- [ ] Verify functionality and responsiveness.
