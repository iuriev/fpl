# Tasks: Compact Player Cards and Scrollable Layout for SquadScreen

- [ ] Modify `web/src/screens/SquadScreen/SquadScreen.module.css` to add sticky positioning *
- [ ] Add `overflow-y: auto` and `min-height` for small screens in `SquadScreen.module.css` *
- [ ] Verify changes with `npm run test -w web`
- [ ] Run linting `npm run lint -w web`
- [ ] Archive the change
