# Tasks: Monobank donation banner (MON-03)

## 1. Config and component

- [x] 1.1 `readDonationUrl()` with default Monobank jar URL and env overrides
- [x] 1.2 `DonationBanner` — external link, gradient animation, reduced-motion fallback
- [x] 1.3 Copy keys + `web/.env.example`

## 2. Sidebar integration

- [x] 2.1 `TeamInfoPanel` — scrollable `.panelBody` + pinned `.donationWrap`
- [x] 2.2 `Drawer` — flex child fills height
- [x] 2.3 Skeleton includes donation footer when URL configured

## 3. Finish

- [x] 3.1 Unit tests (donation lib, banner, TeamInfoPanel)
- [x] 3.2 Backlog MON-03 marked shipped
